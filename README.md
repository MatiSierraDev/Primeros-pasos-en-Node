# Notas
Primer programa en Node.

```
Opciones:

  --help     Muestra ayuda [booleano]
  --version  Muestra número de versión [booleano]
  -b, --base     Es la base de la tabla a multiplicar [número] [requerido]
  -l, --listar   Muestra la lista en consola [booleano] [defecto: false]
  -h, --Hasta    Muestra la tablas hasta el numero ingresado [número] [defecto: 10]

```